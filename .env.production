# Production Environment Variables
NODE_ENV=production
PORT=5000

# MongoDB Atlas Connection (you'll need to replace with your actual connection string)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/education_system

# JWT Secret (generate a strong secret)
JWT_SECRET=your_super_secure_jwt_secret_here_32_characters_minimum

# CORS Origin (replace with your frontend domain)
CORS_ORIGIN=https://your-frontend-domain.netlify.app